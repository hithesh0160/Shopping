name: Shopping Maven CI

on:
  push:              # Runs on push to main branch
    branches:
      - main
  workflow_dispatch: # Allows manual trigger from GitHub UI
    inputs:
      custom_message:
        description: 'Optional custom message for Telegram'
        required: false
        default: ''

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Run Maven and capture filtered output
        id: capture_output
        working-directory: ./shop
        run: |
          OUTPUT="$(mvn -B clean test 2>&1)"
          echo "$OUTPUT"
          echo "✅✅✅ MAVEN BUILD DONE. CAPTURE COMPLETED ✅✅✅"

          # Extract output after test suite starts
          FILTERED_OUTPUT="$(echo "$OUTPUT" | awk '/\[INFO\] Running TestSuite/ {found=1; next} found')"
          
          # Truncate to last 20 lines
          TRUNCATED_OUTPUT="$(echo "$FILTERED_OUTPUT" | tail -n 20 | sed 's/`/\\`/g')"
          
          echo "BUILD_OUTPUT<<EOF" >> $GITHUB_ENV
          echo "$TRUNCATED_OUTPUT" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Send Telegram Message on Success
        if: ${{ success() }}
        run: |
          CUSTOM_MESSAGE="${{ github.event.inputs.custom_message }}"
          if [[ -z "$CUSTOM_MESSAGE" ]]; then
            MESSAGE="✅ *Build Success* for \`${{ github.repository }}\`!\n\n🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})\n\n📝 Output (last 20 lines):\n\`\`\`\n${{ env.BUILD_OUTPUT }}\n\`\`\`"
          else
            MESSAGE="$CUSTOM_MESSAGE"
          fi
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            --data-urlencode chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode text="$MESSAGE" \
            -d parse_mode=Markdown

      - name: Send Telegram Message on Failure
        if: ${{ failure() }}
        run: |
          MESSAGE="❌ *Build Failed* for \`${{ github.repository }}\`!\n\n🔗 [View Run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})\n\n📝 Output (last 20 lines):\n\`\`\`\n${{ env.BUILD_OUTPUT }}\n\`\`\`"
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            --data-urlencode chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode text="$MESSAGE" \
            -d parse_mode=Markdown
